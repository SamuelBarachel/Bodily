rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // All Firestore access goes through the Express API server using
    // Firebase Admin SDK, which bypasses these rules entirely.
    // Direct client-side access is therefore denied for all documents.

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
